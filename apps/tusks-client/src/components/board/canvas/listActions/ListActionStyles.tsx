import styled from "styled-components"

export default styled.div`
  max-width: 300px;
  display: grid;

  .back-btn {
    position: absolute;
    top: 14px;
    left: 21px;
  }

  .archive-btn {
    justify-content: center !important;
  }
`
