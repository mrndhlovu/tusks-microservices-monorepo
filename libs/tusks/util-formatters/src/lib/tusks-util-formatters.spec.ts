import { tusksUtilFormatters } from './tusks-util-formatters';

describe('tusksUtilFormatters', () => {
  it('should work', () => {
    expect(tusksUtilFormatters()).toEqual('tusks-util-formatters');
  });
});
